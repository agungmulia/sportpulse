-- SportPulse PostgreSQL initialization
-- Extensions
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE EXTENSION IF NOT EXISTS unaccent;

-- Full-text search configuration for Indonesian
-- Prisma will handle schema via migrations
